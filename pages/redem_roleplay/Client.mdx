# Client

## isPed
```lua
exports.redem_roleplay:isPed(param)
```

- param: `string`
    - `cid`, `job`, `jobgrade`, `group`

## setStress
```lua
exports['vc-hud']:setStress(amount)
```
- amount: `number` 
    - `10` add 10 stress points
    - `-10` remove 10 stress points
    - max `100`